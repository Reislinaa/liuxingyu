# 流星语 · 网站设计规范（DESIGN.md）

> 用途：本文件是给任何 AI 阅读的设计系统规范。无论使用哪个模型，
> 读完全文即可按统一的视觉语言生成 / 评审 / 修改本项目的 UI。
> 对标：typeless.com / shandianshuo.cn / sayso.cn
> 方向：温暖极简的 AI 语音输入工具官网

---

## 1. 视觉主题与气质

- **温暖极简（warm minimal）**：暖白底 `#FAFAF8`，深墨文字，珊瑚流星 accent。
- **克制是最高优先级**：元素越少越好，大留白，一屏一个视觉焦点。
- **"AI 味"禁止清单**：三色霓虹渐变铺满、毛玻璃滥用、卡片套卡片、
  万能圆角+大阴影、发光椭圆、emoji 当图标、无意义装饰动画。
- **记忆点**："流星语"= 说出来，即成文。输入像流星（快、亮、准）。
  流星意象只通过克制的光晕与渐变表达，不做夸张特效。
- **产品定位**：AI 语音输入法，不是 AI 产品推广平台/产品库。

## 2. 色彩系统

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#FAFAF8` | 页面背景（暖白） |
| `--bg-2` | `#FFFFFF` | 区块交替背景 |
| `--surface` | `#FFFFFF` | 卡片表面 |
| `--surface-2` | `#F4F3EF` | 次级表面 |
| `--surface-3` | `#EAE9E5` | 轨道/弱背景 |
| `--border` | `rgba(28, 28, 30, 0.08)` | 极细边框 |
| `--border-strong` | `rgba(28, 28, 30, 0.16)` | 强调边框 |
| `--text` | `#1C1C1E` | 主文字（深墨，非纯黑） |
| `--text-2` | `#5A5A5E` | 次级文字 |
| `--text-light` | `#8E8E93` | 弱文字 |
| `--primary` | `#E85D4E` | 品牌主色（珊瑚流星色），用于 CTA/选中态/品牌元素 |
| `--primary-dark` | `#C94A3D` | 主色深态 |
| `--primary-light` | `#F07A6D` | 主色浅态 |
| `--secondary` | `#0F766E` | 少量点缀（青绿） |
| `--gold` | `#D97706` | 流星尾焰金 |

**规则**：
- 60-30-10：60% 中性背景留白、30% 文字/边框、10% 强调色。
- 强调色**必须稀缺**才有效，禁止大面积铺色。
- 品牌渐变：`linear-gradient(135deg, #E85D4E 0%, #F59E0B 100%)` 仅用于重点强调。
- 不用纯白纯黑（`#fff`/`#000` 仅用于极少高亮）。

## 3. 排版系统

- 字体：`Bricolage Grotesque` / `Noto Sans SC`（项目已有）。
- 大标题用**负字距**（`letter-spacing: -0.03em ~ -0.045em`），字号 `clamp()` 流式。
- **字重克制**：正文 400，强调 500/600，最高 700（禁止 800 以上粗体）。
- 三级层级：超大标题（2.9rem~6.8rem）/ 区块标题（2rem~2.6rem）/ 正文（1rem）。
- 大厂心法：**display 字号必须紧凑（负字距），正文行高放宽（1.6~1.85）**。

## 4. 组件规范

- **按钮**：
  - 主按钮：深墨实底白字（`--text` 背景）
  - 强调按钮：珊瑚渐变 `--grad-brand` 白字
  - 次按钮：白底细边框
  - 圆角 999px（胶囊形）
  - hover 轻微上浮 `translateY(-2px)` + 轻阴影，不做霓虹发光
- **卡片**：白底、`1px` 细边框、圆角 16~20px、轻阴影；
  hover 上浮 4~6px，顶部可加一条品牌色细线（渐变）作为点亮。
- **区块**：交替 `--bg` / `--bg-2` 背景；区块标题前加一行
  **英文短语小标**（uppercase，如 `CORE CAPABILITIES`，品牌色，字距 2~3px）。
- **图标**：线稿 SVG（stroke=1.5，单色），**禁止 emoji 当图标**。

## 5. 布局原则

- 容器最大宽度 1180px，两侧 28px 内边距。
- 间距用 **8pt 标尺**（8/16/24/32/48/56/72/120）。
- 区块垂直留白 120px，移动端 72px。
- 每屏只有一个视觉重心；Hero 用三段式（顶标/主内容/底提示）避免内容堆顶。

## 6. 动效规范

- 用途优先级：**有含义的动效 > 装饰动效**。动效必须服务内容
  （如：滚动展示平台、线条汇聚表达"思想→文字"、速度对比表达卖点）。
- 缓动：入场用 `--ease-out: cubic-bezier(0.16, 1, 0.3, 1)`。
- **滚动渐入**：`.reveal` 类实现，优先 CSS transition，复杂场景可用 GSAP ScrollTrigger。
- 组件卸载必须清理监听器与动画。
- 尊重 `prefers-reduced-motion`。
- 动画克制：单段时长 0.3~0.6s，stagger 0.04~0.08，禁止夸张回弹。

## 7. 大厂对标工作流（给 AI 的自检流程）

生成或修改 UI 前，先问自己：
1. 这个页面一屏的**视觉焦点**是什么？其他元素是否在让路？
2. 我用了几个强调色？**超过 1 个就该收敛**。
3. 每个动画是否**讲了一件事**？删掉后页面是否更干净？
4. 图片/截图是否是**产品本身**（真实 UI），而不是装饰画？
5. 对比参考：typeless（亮色极简长滚动）、shandianshuo（中文排版克制）、sayso（温暖 accent）。

**评审清单（改稿前过一遍）**：
- [ ] 无三色渐变铺满、无发光椭圆、无 emoji 图标
- [ ] 标题负字距、字重 ≤700、大留白
- [ ] 强调色仅出现在 CTA/选中态
- [ ] 动效有含义、有清理、尊重 reduced-motion
- [ ] 内容与 AI 语音输入法强相关，无 AI 产品推广/产品库/入驻等残留

## 8. 反模式（Don't）

- ❌ 三色霓虹渐变（靛蓝+青+粉铺满）——那是 AI 味重灾区
- ❌ 深色背景 + 发光文字/椭圆
- ❌ 纯黑 `#000` / 纯白 `#fff` 大面积使用
- ❌ emoji、通用圆角卡片套卡片、毛玻璃滥用
- ❌ 无意义的粒子/漂浮/旋转装饰
- ❌ 为炫而炫的动画（弹性、扭曲、大幅位移）
- ❌ AI 产品推广平台相关内容（产品库、入驻、初创公司展示等）

## 9. 页面结构

- **首页（home）**：Hero + MindSection + Features + Showcase + PlatformSection + Footer
- **功能页（features）**：核心能力 + 使用场景 + 四步开始 + CTA
- **下载页（download）**：平台选择 + FAQ
- **关于页（about）**：价值观 + 发展历程 + 联系 + CTA
- **法律页（privacy/terms）**：隐私政策 / 服务条款

## 10. 语音输入产品文案原则

- 核心 slogan：**说出来，即成文**
- 卖点：快（语音速度）、准（智能整理）、自然（润色后可直接使用）
- 场景：聊天、邮件、文档、代码注释、会议记录、跨语言沟通
- 避免：聊天机器人、AI 助手、内容生成平台等定位模糊表述
