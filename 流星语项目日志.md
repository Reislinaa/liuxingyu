# 🌠 流星语 · 项目完整日志

> 这是一个持续维护的项目日志文档。每次修改代码后，都应在本文件末尾追加更新记录。
> 最后更新：2026-08-26

---

## 一、项目基本信息

| 项目 | 内容 |
|------|------|
| **项目名称** | 流星语 |
| **项目定位** | AI 产品推广/发现平台（给初创 AI 小公司引流） |
| **品牌口号** | 让好 AI 产品被看见 |
| **官网地址（GitHub Pages）** | https://Reislinaa.github.io/luixingyu/ |
| **GitHub 仓库** | https://github.com/Reislinaa/luixingyu |
| **仓库用户** | Reislinaa |
| **仓库名** | luixingyu |
| **项目代码目录** | F:\网站尝试\ai-input-method |
| **一键启动（本地）** | F:\网站尝试\启动网站.bat |
| **一键部署（GitHub）** | F:\网站尝试\ai-input-method\部署到GitHub.bat |

---

## 二、技术栈与环境

### 前端
- **框架**：React 19 + Vite 5
- **语言**：JSX / CSS
- **入口**：`src/main.jsx` → `src/App.jsx`
- **页面**：首页 / 介绍 / 产品服务 / 下载 / 关于我们 / 入驻申请 / 产品详情 / 隐私政策 / 服务条款

### 后端（代码已写好，但 GitHub Pages 无法运行）
- **框架**：Node.js + Express 4
- **入口**：`server/index.js`
- **AI 服务**：`server/aiService.js`（支持对接 OpenAI 兼容大模型，未配置 Key 时用本地词库兜底）
- **数据存储**：JSON 文件（`server/data/`），用户数据 + 请求日志

### 环境要求
| 软件 | 用途 | 地址/版本 |
|------|------|----------|
| **Node.js** | 运行前端构建和后端 | 需 Node 18+（本机 v24.x），nodejs.org |
| **npm** | 包管理器 | 随 Node 自带 |
| **Git** | 版本控制/部署 | 本机已装（git 2.55.0），git-scm.com |
| **GitHub** | 代码托管 + 静态托管 | github.com |
| **（可选）gh CLI** | GitHub 命令行 | 本机未装，未使用 |

---

## 三、网站结构与功能

### 导航栏（5 个独立页面）
1. **首页** - Hero（流星引擎 + 地面剪影）+ 为什么选择流星语
2. **介绍** - 发展历程时间线 + 价值观
3. **产品服务** - AI 产品库（搜索/分类筛选/产品卡片）+ 详情页
4. **下载** - 四大平台下载卡片 + FAQ
5. **关于我们** - 平台介绍 + 联系方式

### 其他页面
- **入驻申请** - 表单（前端演示，未接后端）
- **隐私政策 / 服务条款** - 合规页面
- **产品详情** - 单产品展示（访问官网按钮）

### 核心特性
- **深色 UI**：`#0B0B18` 背景 + 极光紫蓝 `#7C7CFF` 渐变
- **流星 Canvas 引擎**：普通流星 / 火流星（线状余迹+碎闪）/ 流星雨 / 照亮大地
- **多样动画**：Reveal 6 种变体（上移/左右滑入/缩放/模糊聚焦/纯淡入）+ 页面切换过渡
- **移动端适配**：响应式，手机单列布局
- **登录/注册**：前端弹窗有，后端接口已写好但线上不可用

---

## 四、从零开始遇到的问题与解决（重要！）

### 问题 1：本地白板，网页显示不出来（重大坑）
**现象**：浏览器打开 `localhost:5173` 一直白板，怎么刷新都没用。
**排查过程**：
- 检查服务端口：前后端都在监听
- 检查 HTML：index.html 正常
- 检查 Vite 编译：报错 `Failed to resolve import "react/jsx-runtime"`
- 发现只转换 3 个模块就失败

**根本原因**：`react` 和 `react-dom` 没有正确安装到 node_modules！
**解决**：重新 `npm install react react-dom`，确认 `jsx-runtime.js`、`client.js` 存在。
**验证**：转换从 3 个模块 → 44 个模块，构建成功。
**教训**：npm install 时若遇到 `esbuild 脚本未批准`、node-gyp safe-delete 中断，可能漏装关键依赖，需检查。

### 问题 2：中文路径在命令行编码错乱
**现象**：`cd F:\网站尝试\...` 时中文被解析成乱码（"网站尝试"→"缃戠珯灏濊瘯"）。
**原因**：PowerShell 命令脚本文件编码（GBK vs UTF-8）问题。
**解决**：创建 ASCII 名称的目录链接 `F:\ai-im` → 项目目录，通过 `F:\ai-im` 操作。
**当前状态**：`F:\ai-im` 仍是指向项目的链接，可继续用。

### 问题 3：npm install 的 safe-delete shim 拦截
**现象**：删除 node_modules 大量文件时被工具安全机制中断。
**解决**：用 `cmd /c "rd /s /q node_modules"` 手动删除，或用 npm 重新安装。
**教训**：node-safe-delete-shim 拦截 `fs.rmSync`，脚本里避免用 rmSync 删目录。

### 问题 4：sqlite3 原生编译失败
**现象**：sqlite3 依赖安装失败（原生模块编译问题）。
**解决**：改用**纯 JSON 文件存储**（`server/store.js`），不依赖原生编译，更易部署。

### 问题 5：Vite 只监听 IPv6 [::1]
**现象**：netstat 显示 5173 端口只监听 IPv6。
**排查**：浏览器用 localhost 访问实际能连上（有 [::1] 连接记录），非根本问题。

### 问题 6：本地环境怎么都打不开（已放弃本地预览）
**现象**：代码验证全部正常，但用户本地浏览器始终白板。
**结论**：判断为本地浏览器/环境连接问题（代理/防火墙/IPv6），**非代码问题**。
**最终方案**：**改用 GitHub Pages 公网部署**，绕开本地环境，用公网 URL 查看。

### 问题 7：重复 export default 导致构建失败
**现象**：构建报错 `Multiple exports with the same name "default"`。
**原因**：MeteorCanvas.jsx 文件末尾多写了一行 `export default MeteorCanvas`（函数声明本身已是默认导出）。
**解决**：删除末尾重复的 export 行。

### 问题 8：GitHub Pages 部署需要认证
**现象**：git push 到 gh-pages 分支时可能遇到认证问题。
**解决**：仓库需为 Public，且本地 git 需配置账号/token。当前已成功部署。

---

## 五、用户的核心要求（务必遵守）

1. **品牌名**：必须叫「**流星语**」，不能改
2. **定位**：AI 内容推广平台，给初创 AI 小公司引流（**不是做 AI 输入法本身**）
3. **导航**：顶部功能栏，每项点击后是**独立的完整页面**（不是滚动跳转）
4. **移动端**：要适配手机
5. **UI 风格**：深色简洁（参考 Moonshot moonshot.cn 的动态和平滑过渡，配色可改）
6. **流星动画**：
   - 有快慢之分，快的浅淡如星光（频率要高），慢的明显如烛火闪烁
   - 轨迹要真实（斜向下落，不是平移）
   - 火流星尾迹是**一条线**：头部亮、越靠尾部越淡直至消失（不要泡泡）
   - 节奏要**随机**，不要规律循环
   - **极光不要了**，改成飘动的云（半遮盖效果）
   - **首屏 1 秒内必须出现大流星**
   - **地面场景**（由 Canvas 绘制）：建筑（窗户常亮）+ 工作的人/徒步者 + 风景山丘，全部是**黑色剪影**
   - **局部照亮**：只有流星附近的地面才被照亮，**流星越大照得越远**
   - 平时地面黑的看不到，流星来时才照出建筑/人的黑色轮廓（像影子），窗户则一直透着光
7. **文件位置**：所有文件在 F 盘，**绝不放 C 盘**
8. **部署**：先 GitHub Pages 静态版，后续买服务器部署完整版

---

## 六、部署方案

### 当前：GitHub Pages（静态版）
- **地址**：https://Reislinaa.github.io/luixingyu/
- **方式**：`dist/` 静态构建产物推送到 `gh-pages` 分支
- **base 路径**：`/luixingyu/`（子路径部署）
- **可用**：所有页面、动画、UI
- **不可用**：登录/注册、AI 接口（无后端）

### 将来：完整版（计划中）
- **服务器**：计划购买腾讯云（国内，需 ICP 备案）
- **域名**：计划注册 .cn 域名
- **流程**：注册账号→实名→买域名→买服务器→ICP备案(7~20天)→部署前后端
- **备案注意**：网站名称用中性描述，地址精确到门牌号，留意短信核验
- **所需材料**：身份证、手机号、邮箱、服务器、域名

### 部署命令
```bash
# 手动部署到 GitHub Pages
cd F:\ai-im
node scripts/build-with-base.mjs /luixingyu/   # 构建
node scripts/deploy-gh-pages.mjs Reislinaa luixingyu  # 推送
```

---

## 七、关键文件清单

```
F:\网站尝试\
├── 启动网站.bat                    # 本地一键启动
└── ai-input-method\               # 项目主目录
    ├── 部署到GitHub.bat           # 一键部署 GitHub Pages
    ├── package.json               # 依赖配置
    ├── vite.config.js             # Vite 配置（base 路径）
    ├── index.html                 # 入口
    ├── README.md                  # 项目说明
    ├── public/favicon.svg         # 网站图标
    ├── scripts/                   # 部署/构建脚本
    │   ├── build-with-base.mjs    # 构建（带 base）
    │   └── deploy-gh-pages.mjs    # 推送 gh-pages
    ├── server/                    # 后端（GitHub 上不跑）
    │   ├── index.js               # Express 入口
    │   ├── aiService.js           # AI 服务
    │   └── store.js               # JSON 存储
    ├── src/
    │   ├── main.jsx               # React 入口
    │   ├── App.jsx                # 页面路由
    │   ├── index.css              # 全局主题
    │   ├── context/AuthContext.jsx
    │   ├── components/            # 通用组件
    │   │   ├── Navbar / Hero / Features / Footer
    │   │   ├── MeteorCanvas.jsx   # 流星引擎（Canvas）
    │   │   ├── Reveal.jsx         # 滚动动画
    │   │   ├── AuthModal / LegalPage
    │   └── pages/                 # 独立页面
    │   │   ├── Home / Intro / Product / Download / About
    │   │   ├── ProductDetail / Join / LegalPage
    │   └── data/aiProducts.js     # 产品数据
```

---

## 八、开发日志（按时间记录）

### 2026-08-26（初始搭建）
- 确定网站定位（原为 AI 输入法）
- 搭建 React+Vite+Express+SQLite 框架
- 因 sqlite3 编译问题改为 JSON 存储
- 遇到白板问题（react 缺失），修复
- 遇到中文路径编码问题，建 F:\ai-im 链接
- 本地预览始终无法打开，改用 GitHub Pages 部署
- 部署成功：https://Reislinaa.github.io/luixingyu/

### 2026-08-26（品牌定位调整）
- 用户明确：**不是 AI 输入法，而是 AI 内容推广平台**（给初创 AI 公司引流）
- 品牌名定为「流星语」
- 重构首页为 AI 产品发现平台
- 开发产品库、产品详情、入驻申请页面
- 加入移动端适配

### 2026-08-26（多页面重构）
- 用户要求：导航每项是**独立完整页面**，不是滚动跳转
- 移除所有锚点滚动链接（#features 等）
- 开发独立：介绍/产品服务/下载/关于我们 页面

### 2026-08-26（UI 升级 + 动效）
- 参考 Moonshot（moonshot.cn）设计
- 深色高对比配色 + 平滑缓动 cubic-bezier(0.16,1,0.3,1)
- 加入滚动渐入动画、页面切换过渡、按钮流光
- 配色保持流星语紫色系

### 2026-08-26（流星动画迭代）
- v1：CSS 光带平移 → 用户反馈轨迹不对
- v2：改角度修正轨迹 → 用户反馈太规律、尾迹像泡泡
- v3：Canvas 引擎，随机节奏 + 线状余迹 + 多样动画
  - 普通流星/火流星/流星雨/照亮大地（地面 SVG 剪影）
  - 指数式随机间隔、Reveal 6 种动画变体
- v4（当前）：重写流星引擎，按用户细则
  - **去掉极光 glow**（用户：极光不要了）
  - **加飘动的云**（半透明、前后两层，可半遮盖流星）
  - **火流星尾迹改成一条线**：头部亮粗、越靠尾部越淡直至消失（之前是泡泡，已彻底移除）
  - **首屏 1 秒内必出大流星**（nextFireballAt = 0.3~0.9s）
  - **小流星频率提高**（指数间隔均值 3.2s→1.8s，连发概率 0.35→0.5）
  - **地面场景改由 Canvas 绘制**：建筑（带常亮窗户）+ 人物（工作的人/徒步者）+ 远景山丘，全部为黑色剪影
  - **局部照亮**：只有流星附近的地面才被照亮；**流星越大，照亮半径越远**（lightR 随 size 缩放）
  - **窗户常亮**：无论是否被照亮，窗户一直透着暖光；流星来时才照出建筑/人的黑色轮廓（影子）
  - 移除 .hero-ground SVG，全部在 MeteorCanvas 内绘制
  - 关键文件：`src/components/MeteorCanvas.jsx`（v4）

### 2026-08-26（首页与流星综合调整）
- **浏览器标签栏**：`index.html` 标题原为 `流星语 · AI 智能输入法`，改为 `流星语 · 用星光为每一次表达提效`（去掉"AI输入法"字样，用户反馈栏里显示 ai输入法）
- **流星速度整体调慢**：原 speed 900~1600；火流星仅 360~560。现重新分级：
  - 暗弱 faint 700~950（快、极淡）
  - 普通 normal 560~820
  - 明亮 bright 420~600
  - 慢流星 slow 300~430
  - 火流星 fireball 150~240（**最慢、最大、最亮、照最远**）
  - 肉眼可见五类区分（速度/大小/亮度）
- **极光回归但压暗**：新增 `initAurora`/`drawAurora`，低亮度（peak 0.05~0.12）+ 模糊 + 缓慢呼吸，可看出云与天色，不过亮
- **首页标题重写**（带美意、关联"流星雨照亮大地 + 给小公司做推广"，字少不遮挡 UI）：
  - tag：`— 为成长中的企业，点亮被看见的星光 —`
  - 大字：`流星所至` / `照亮万物表达`（渐变，克制）
  - 副标题：点明帮小公司被看见 + 流星照亮意象
  - **删除统计数字**（50+ / 10万+ / 8大 等，用户：太土）
- **鼠标互动光效**：
  - Hero 加 `.hero-mouse-glow` 柔光层随光标移动
  - 按钮 `.btn::after` 高光跟随光标（JS 设置 --bx/--by）
- 关键文件：`src/components/Hero.jsx`、`Hero.css`、`MeteorCanvas.jsx`、`index.html`

---

## 九、待办事项（To-Do）

- [ ] 购买腾讯云服务器（100-500元/年预算）
- [ ] 注册 .cn 域名（约30元/年）
- [ ] ICP 备案（7~20天）
- [ ] 部署完整版（前端+后端+数据库）
- [ ] 绑定域名 + HTTPS 证书
- [ ] 对接真实大模型 API（需 API Key）
- [ ] SEO 优化（让搜索引擎能搜到）
- [ ] 录入真实 AI 初创公司产品数据（当前是演示数据）
- [ ] 入驻表单接后端（当前前端演示）

---

## 十、温馨提示

- 本项目代码在 **F 盘**，绝不写入 C 盘
- GitHub Pages 是静态版，登录/AI 功能需等服务器部署
- 流星动画是 Canvas 实现的，改动在 `src/components/MeteorCanvas.jsx`
- 产品数据在 `src/data/aiProducts.js`，添加产品改这里
- 部署脚本：修改代码后构建 + 推送即可更新线上
